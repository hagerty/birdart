[project]
name = "birdart-frame"
version = "0.1.0"
description = "Turn BirdWeather detections into artwork for a Samsung Frame TV."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
